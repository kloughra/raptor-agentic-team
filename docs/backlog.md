# Backlog

## Sprint 16 — Planned
Theme: **Autonomy substrate** — advance the autonomous-Raptor north star (run sprints → notify me out-of-band → I stay informed → asks for guidance when the backlog is dry) and cut the manual-shepherding surface (Sprint 15 took 53 `resume_sprint` calls).
- [ ] notification-egress: Native outbound notification hook so a running sprint pings the user out-of-band at key events — checkpoint reached, escalation raised, sprint complete, sprint failed. Start minimal: a pluggable notification channel with a Slack incoming-webhook driver (POST to a config'd URL — no OAuth/bot), configured via `notifications.slack_webhook_url` in `~/.raptor/config.json` and PARSED in `loadConfig` (avoid the `config-keys-parsed-vs-declared` dead-plumbing class — add a parsed-vs-declared conformance test). Message payload MUST key off PERSISTED sprint state / git, never agent self-report (pairs with `surface-tool-errors-to-openstory` this sprint), so a "failed" ping can't be counterfeit; include checkpoint context + the exact `resume_sprint` command to run. Pluggable channel with a Slack driver this sprint and Discord as a future driver — supersedes the `discord-integration` Inbox item. Foundational substrate for the autonomous north star. — source: user north-star design session 2026-07-07; promoted Inbox → Sprint 16 (Autonomy substrate) 2026-07-12
- [ ] surface-tool-errors-to-openstory: Raptor tools return failures as `{status: "error"}` strings, not exceptions, so OpenStory's error detector reports `[]` for sessions that clearly failed. Either throw on failure or emit a structured error event so failed sessions are detectable. Trust substrate for `notification-egress` (this sprint): a notification payload that keys off persisted state needs failures to be first-class, not swallowed. — source: session ea5bc6fd 2026-04-26; promoted Ready → Sprint 16 (Autonomy substrate) 2026-07-12
- [ ] reset-sprint-tool: First-class `reset_sprint` MCP tool to clear escalated/failed state. Today users must `rm ~/.raptor/{project}/sprint-N.json` by hand to escape circuit-breaker escalation. Cuts the manual-intervention surface the autonomy north star aims to eliminate. — source: session ea5bc6fd 2026-04-26; promoted Ready → Sprint 16 (Autonomy substrate) 2026-07-12

## Sprint 15 — Planned
- [x] push-before-merge: complete — see Done section. PR #34 merged 2026-07-08.
- [x] retro-section-matching-rarely-hits-target: complete — see Done section. PR #35 merged 2026-07-08.
- [x] user-actionable-failure-class: complete — see Done section. PR #36 merged 2026-07-08.
- [x] blocker-marker-false-positive-in-agent-output: complete — see Done section. PR #37 merged 2026-07-08.

## Sprint 14 — Planned
- [x] adversarial-verifier-review-gate: complete — see Done section. PR #32 merged 2026-07-07 (merged manually by the user at the demo checkpoint, merge commit 4924d8c; step 9 correctly treated the already-merged PR as success per the Sprint 13 state-verifying merge rule). Demo accepted 2026-07-07 (780/780 green live, Part 1 + Part 2 shipped, Part 3 vacuous by design). Follow-up filed at step 10: `invalid-model-signature-class` (Inbox). Verified still queued in Ready for Sprint 15: `billing-error-signature-class`, `push-before-merge`.

## Sprint 13 — Planned
- [x] sprint-completes-despite-failed-merge: complete — see Done section. PR #29 merged 2026-07-06 (merged manually by the user at the demo checkpoint, merge commit c07eeb9; step 9 correctly treated the already-merged PR as success per stakeholder direction). Demo accepted 2026-07-06; follow-ups filed at step 10: `push-before-merge` (Ready, Sprint 14 candidate), `billing-error-signature-class` (Ready), `merge-failure-short-circuit` (Inbox), `merge-step-already-merged-idempotency` (Inbox).
- [x] retro-improvements-not-applied: complete — see Done section. PR #30 merged 2026-07-06. Its own step 13 was the first production run of the fix: 3 adopted proposals with inexact Section targets were fallback-preserved with attribution and reported honestly (0 applied, 3 at fallback) instead of silently dropped; entries relocated to their intended sections in this close-out.

## Sprint 12 — Planned
- [x] progress-aware-circuit-breaker: complete — see Done section. PR #27 merged 2026-07-06. First sprint to complete all 13 orchestrated steps end-to-end; step-9 merge required manual completion (see sprint-completes-despite-failed-merge, now Sprint 13) and step-13 retro application was manual (see retro-improvements-not-applied, now Sprint 13; TEAM.md rules applied via PR #28).

## Sprint 11 — Planned
- [x] expected-outputs-glob-resolution: complete — see Done section. PR #24 merged 2026-07-06 via hotfix workflow (bootstrap paradox: the sprint's own step-3 validation depended on the bug under fix, so the orchestrator path could never pass — see `sprint-11-write-tests-escalation` post-mortem in Inbox).
- [x] artifact-injection-directory-handling: complete — see Done section. PR #25 merged 2026-07-06 via hotfix workflow (companion to PR #24; includes provenance correction — the crash was in `buildStepContext`, not `artifact-injection.ts`).

## Sprint 10 — Planned
- [x] orchestrator-recovery-after-mixed-completion: complete — see Done section. PR #21 merged 2026-06-12 (checkbox reconciled during Sprint 13 prep; the merge landed but this line was never checked).

## Sprint 9 — Planned
- [x] dev-loop-rebuild-friction: complete — see Done section. PR #17 merged 2026-05-04.
- [x] live-claude-smoke-test: complete — see Done section. PR #19 merged 2026-05-04 via hotfix workflow (orchestrator could not re-engage escalated feature; bundled production hardening shipped first as PR #18).

## Sprint 8 — Planned
- [x] multi-feature-sprint-dispatch: complete — see Done section. PR #15 merged 2026-04-27.

## Ready (prioritized, next sprint)
- persist-feedback-across-retries: Directional user/critic feedback is injected only on attempt 1 of a step and silently dropped on attempts 2–3 — verified at `runner.ts:832` (single-feature) and `runner.ts:1448` (multi-feature), both gated on `attempt === 1`. Per Reflexion (arXiv 2303.11366), feedback should persist as an episodic reflection buffer conditioning EVERY retry, not just the first. Add a per-step reflection buffer to `sprint-N.json` that accumulates directional feedback across all attempts (with a retention policy to avoid context overflow on long sprints), and bias the QA acceptance gate toward false-negative over false-positive ("an agent can self-reflect on a failing test but cannot recover from a falsely-passing one"). Closely related to escalate→resume. — source: loop-engineering research 2026-06-19
- backlog-format-error-with-example: When `run_sprint` rejects a backlog with "No backlog items found for sprint N" (tools.ts:687), include the expected format inline (`## Sprint N` / `- [ ] slug: description`) and a doc link. dora-metrics user spent ~3min in an edit→run→error loop reverse-engineering the parser — source: session ea5bc6fd 2026-04-26
- adopt-project-git-init: `adopt_project` should detect non-git directories and either auto-init (with prompt) or fail with a clear "run `git init` first" message. Today the failure surfaces as an opaque simple-git error — source: session ea5bc6fd 2026-04-26
- config-keys-parsed-vs-declared: `RaptorConfig` (config.ts) declares `testConfig`, `codebaseContext`, `artifactInjection`, and `scopeNarrowing`, but `loadConfig` never parses them — users setting those keys in `~/.raptor/config.json` are silently ignored. Same defect class as the dead `timeouts` plumbing fixed by CB-5 in Sprint 12, which sat unnoticed for six sprints. Fix: parse every declared key (or delete unparsed keys from the interface) and add a conformance test asserting parsed-vs-declared parity so the two can't drift again. — source: Sprint 12 demo tech-debt flag 2026-07-06
- hotfix-workflow-tool: Add a `run_hotfix` MCP tool that enforces a lightweight SDLC: create hotfix branch → implement + test → PR → user approval → merge. Tracked in sprint state like a mini-sprint. Prevents untracked code changes.
- pre-flight-branch-check: Before any agent writes code, verify it's on a tracked branch (sprint or hotfix). If on main, refuse to proceed and prompt for branch creation. Enforced in `spawnAgent` and runner loop.
- change-proposal-tool: Add a `propose_change` MCP tool that creates a backlog entry and routes to the correct workflow (sprint vs hotfix) based on scope. Forces all changes through tracking before any code is written.
- resource-aware-agent-scheduling: Add concurrency limits to parallel agent execution. Cap parallelism (e.g., max 2 agents) or detect CPU contention and queue excess agents
- checkpoint-resume-for-subagents: Pre-summarize completed discovery work so retries skip re-reading all specs and jump straight to generation

## Inbox (unprioritized)
- merge-failure-short-circuit: Wire merge-step failures into `decideAfterFailure` so a deterministic merge failure (e.g. branch protection) doesn't burn 3 identical attempts before escalating. Deferred by Architect ruling at Sprint 13 (OQ2): merge attempts cost seconds, not minutes, so the retry burn is cheap today — and C5 already enriches merge FailureRecords with `classification` + `signature`, so a future short-circuit is state-compatible with Sprint 13+ state files. Revisit alongside `billing-error-signature-class` (Ready) since both extend the same classification pipeline. — source: Sprint 13 architecture OQ2 ruling + demo flag 2026-07-06
- merge-step-already-merged-idempotency: Sprint 13 step 9 (both features) encountered PRs already merged manually by the user at the demo checkpoint (PR #29 merge commit c07eeb9; PR #30 likewise) and — per stakeholder direction in the demo-feedback checkpoint — treated the "pull request already merged" response as success, since the code is verifiably on main. Verify `executeMerge` handles this natively (detect PR state `MERGED` → success, no failure recorded); if the Sprint 13 pass relied on ad-hoc shepherding, make it first-class with a test so a manually-merged PR can never trip the merge retry/escalation path. — source: Sprint 13 demo-feedback checkpoints 2026-07-06/07
- pr-review-feedback-routes-to-wrong-step-code: The TEAM.md routing rule (Sprint 12 retro proposal 2, applied via PR #28) tells the Engineer what a revision must look like, but the ORCHESTRATOR still routes a pr-review request-changes to step 6 (Open PR): `resumeSprint`'s checkpoint branch resets and re-runs the checkpoint's own step, whose primary task (PR exists) is already satisfied — observed live in Sprint 12: first revision attempt produced a zero-code-change duplicate handoff (30b80ec). Code fix: request-changes at the pr-review checkpoint should re-enter step 5 (Implement) as a revision task carrying the verbatim review feedback, reusing the PR/branch; treat a zero-diff revision as a failed attempt (signature-classifiable for the CB-1 short-circuit). — source: Sprint 12 dogfood 2026-07-06
- salvage-checkpoint-review-flag: Should `completedVia: "salvage"` force the next checkpoint to flag the step for explicit human review? Deferred at Sprint 12 demo (spec Open Question 6). Current behavior accepted for now: salvage only fires when ALL expected outputs exist on disk and pass the glob gate, and the `completedVia: "salvage"` marker is persisted in sprint state either way — fully auditable after the fact. Revisit if a salvage-completed step ever ships a defect that a fresh agent attempt would have caught (precedent: `batch-checkpoints-config` — don't add approval friction until friction data exists). — source: Sprint 12 demo open question #1 (PO triage 2026-07-06)
- sprint-result-status-hardcoded-escalated: The multi-feature finalization guard (`runner.ts:~1818-1825`) sets `state.status = sprintStatus` correctly (which can be `failed`) but the returned `SprintResult.status` is hardcoded to `"escalated"`. The persisted state is right; only the transient return label is wrong, so a `failed` mixed sprint is reported to the caller as `escalated`. Low impact (reporting only). Fix: return `status: sprintStatus`. Flagged as a non-blocking nit at PR review and demo during Sprint 10. — source: Sprint 10 review/demo 2026-06-11
- orchestrator-recovery-after-mixed-completion: When a multi-feature sprint has one feature reach DoD and another escalate (3-attempt circuit-breaker), the orchestrator advances through Steps 10-13 (process-feedback, retro-collect, retro-review, retro-apply) and marks the sprint "in-progress" but `resume_sprint` then refuses with `Sprint is in 'in-progress' status and cannot be resumed`, and `run_sprint` re-issues the same status report without re-engaging the escalated feature. There is no built-in path to send fresh directional `request-changes` feedback to the parked feature — users must either edit `~/.raptor/{project}/sprint-N.json` by hand or hotfix the remaining work outside the sprint (which is what Sprint 9 did for `live-claude-smoke-test` after PR #18 landed the production prerequisite). Fix: when one or more features are in `escalated` status and the rest have completed, allow `resume_sprint --action=request-changes --feature=<slug>` (or implicit if exactly one is escalated) to reset that feature's `attempts=0`, push the user feedback into the next agent invocation, and re-enter the runner at the escalated step. Closely related to the `reset-sprint-tool` Ready item but distinct: that one clears state to start over; this one carries the user's directional feedback into a re-attempt. — source: Sprint 9 live-claude-smoke-test escalation 2026-05-04
- shared-steps-bypass-slug-detection: `runSprintFromStep` runs `detectSprintFeatures` at the top of the function regardless of which step is being resumed. When Sprint 8 was resumed at step 13 (Apply retro improvements — a sprint-shared step that the runner handles deterministically without an agent or slug), the sprint section had been edited to a comment-only state and the dispatcher errored out with "Could not extract feature slug from backlog" before reaching step 13. Required a manual sentinel `- [x]` item in the backlog to unblock. Fix: skip slug detection when resuming at a step > 9 (sprint-shared steps don't need feature dispatch) — source: Sprint 8 step-13 resume failure 2026-04-28
- partial-artifacts-gitkeep-filter: `validateStepOutputs` (runner.ts:75-96) lists every file in expected-output dirs without filtering `.gitkeep`, so `hadPartialArtifacts` is permanently `true` after bootstrap and masks the real signal of whether anything was written — source: dora-metrics adopt-and-run failure 2026-04-26
- agent-assisted-backlog-reformat: When deterministic backlog parsing fails to categorize items (tables, prose, non-standard formats), fall back to spawning a PO agent to reformat. Handles edge cases the regex parser misses — source: post-mortem from hotfix/backlog-reformat-on-adopt
- batch-checkpoints-config: Optional `batchCheckpoints` config knob to collapse per-feature checkpoints in multi-feature sprints into a single batched approval. Deferred at Sprint 8 demo — current per-feature streaming behavior preferred until we see a sprint with >3 features and approval friction is the bottleneck — source: Sprint 8 demo question #3 (PO triage 2026-04-27)
- mcp-remote-hosting: Host Raptor remotely for multi-device access (laptop, desktop, phone) — source: user request
- mcp-github-integration: Create GitHub repos and push during bootstrap — source: user request
- mcp-cicd-setup: Configure CI/CD pipelines in bootstrapped repos — source: user request
- discord-integration: Discord communication channel for team agents — source: user request
- openclaw-evaluation: Evaluate OpenClaw as potential addition — source: user request
- security-permissions-model: Security and permissions model for agent operations — source: user request
- multi-device-sync: Sync project state across devices — source: user request

## Done
- [x] blocker-marker-false-positive-in-agent-output: Line-anchored, fence/quote-aware `[BLOCKER]`/`[ESCALATE]` detection — new pure leaf `src/orchestrator/blocker-marker.ts` with a shared `stripSuppressedLines` primitive reused by both `hasBlockerMarker` (runner.ts) and `git-parser.ts`; markers are only honored at line start, with fenced code blocks and quoted text stripped first, so an agent that merely quotes or discusses the marker (docs, demo narration, decision-pipeline diagrams) no longer trips a real escalation. Conservative on ambiguity (unclosed fence ⇒ suppress the remainder); untrusted input, never throws. 923/923 tests green. Notably dogfooded its own bug: the demo step tripped the false positive twice before the fix (commits b7a2b25, b416021) plus a manual control-byte sanitize in the integration test. PR #37 (Sprint 15)
- [x] user-actionable-failure-class: Third failure classification — `user-actionable` — added to `failure-classification.ts` alongside `transient` (retry helps) and `deterministic` (task is wrong), wired into `decideAfterFailure` to escalate immediately after 1 attempt with a message naming the required user action. Two signature patterns ship together: billing/spend-limit (action = raise the limit at claude.ai/settings/usage) and invalid-model (action = fix `models.byRole`/`models.default` in `~/.raptor/config.json`). Precedence user-actionable → transient → deterministic; one classifier/pipeline across both seams; extensible registry like TRANSIENT_ERROR_PATTERNS. PR #36 (Sprint 15)
- [x] retro-section-matching-rarely-hits-target: Revived the applied-at-target retro path — after the frozen exact-match path, `applyImprovements` now extracts real TEAM.md headings live (fence-aware) and matches a proposal's compound Section value by whole-token contiguous-subsequence containment, so descriptive Section values (e.g. "Roles & Responsibilities → Product Owner") land at their intended heading instead of falling to the attributed Unplaced fallback that swallowed every Sprint 13/14 proposal. Precision over recall (no fuzzy/substring/prefix); fallback, attribution, content-based idempotency, and commit-only-on-change all unchanged. PR #35 (Sprint 15)
- [x] push-before-merge: Pre-merge push in `executeMerge` — the feature branch is pushed to origin (non-forced, single refspec via `simple-git`) before `gh pr merge`, so unpushed local demo/retro commits can no longer cause the Sprint 12 branch-divergence merge failure; a push failure fails the attempt cleanly with `classifyFailure`/`deriveFailureSignature` metadata, feeding the existing step-9 in-place do/while retry/escalation loop. Handles no-remote repos and force-push hazards per Architect ruling. PR #34 (Sprint 15)
- [x] adversarial-verifier-review-gate: Review gates hardened as out-of-loop adversarial verifiers. **Part 1 (real-seam enforcement):** the orchestrator injects an adversarial-gate instruction into the step-7 QA gate at both production seams (`runner.ts:1053` single-feature, `:1815` multi-feature) directing the verifier to hunt test-local reimplementations of the system-under-test, confirm constraint-guarding tests carry a RED-verification note (TEAM.md QA rule 12), bias toward false-negative, and FLAG+FAIL rather than pass silently — enforcement in code, not just process text. **Part 2 (generator ≠ verifier):** optional `model` param on `spawnAgent` → `claude --model` inserted at the front of the options block so the PR-#18 argv tail (`--allowedTools` ordering + `--` end-of-options separator + terminal `taskDescription` positional) is byte-identical when unset; `models` config (`default` / `byRole`) parsed in `loadConfig` via `parseModels` (mirrors `parseTimeouts`, type-guarded, never throws — averts the `config-keys-parsed-vs-declared` dead-plumbing class); `resolveRoleModel` threads the resolved model to every runner spawn site so the QA gate can run on a different model than the Engineer. **Part 3 (bias controls):** vacuous by design — no LLM scoring/judge gate introduced (recorded decision per AC 11, not a silent skip); no judge ensemble built (explicit refutation honored). No baked-in default verifier model (config-only; absent config = today's behavior everywhere), with a different-model recommendation documented in the config reference. 780/780 tests green (44 suites, +24 feature-scoped) asserting the real `spawnAgent` argv and the real step-7 runner prompt with RED-verification notes; no state-schema change; all five existing `spawnAgent` call sites unchanged. PR #32 (Sprint 14)
- [x] retro-improvements-not-applied: Outcome-tracked retro application — applyImprovements returns per-proposal outcomes (applied / applied-fallback / already-present / unplaced) with fence-aware normalized heading matching; unmatched proposals fallback-append to an attributed `## Adopted Retro Improvements (Unplaced)` section instead of silently dropping; shared executeRetroApply behind both runner paths; outcomes persisted to state.retroApply, the retro doc Applied Changes, and a qualified completion message; content-based idempotency. PR #30 (Sprint 13)
- [x] sprint-completes-despite-failed-merge: Truthful sprint completion — `state.status === "complete"` now implies every step is `complete` and the code is actually on `main`. C1: single-feature skip-a-step `continue` replaced with a bounded in-place `do/while` around `executeMerge` (`runner.ts:935-993`), failure accounting and escalation-at-cap preserved; C2: multi-feature dispatcher consumes the previously-discarded `"retry"` outcome from `runMergeStepForFeature`; C3: finalization guard — `complete` unreachable with any non-complete step, guard trips park as `escalated` (resumable, never Sprint-9-style limbo); C4: shared steps 10–13 gated until every feature is terminal at step 9; C5 (additive): merge FailureRecords enriched with `classification`+`signature` via the Sprint 12 `failure-classification.ts` module. 709/709 tests (+28 feature-scoped driving the real `runSprintFromStep` seam, proven RED against the pre-fix control flow: 10 failed pre, 13/13 post). No schema changes, no new deps, historical falsely-complete state files load unchanged. PR #29 (Sprint 13)
- [x] sprint-11-write-tests-escalation: post-mortem record retired from Ready — all three compounding defects it documented (hard wall-clock kill of a still-working agent, transient errors consuming circuit-breaker slots, completed artifacts discarded) were fixed by `progress-aware-circuit-breaker` (PR #27, merged 2026-07-06). Retirement was deferred in the Sprint 12 demo feedback ("retire to Done alongside progress-aware-circuit-breaker once PR #27 merges"); executed at Sprint 13 step 10. Full diagnostic preserved in git history and `docs/sprints/sprint-12-summary.md`
- [x] progress-aware-circuit-breaker: Progress-aware retry machinery — pure `decideAfterFailure` RetryDecision pipeline shared by both retry loops (salvage-complete → transient no-slot retry (cap 5, 15s delay) → no-progress short-circuit on identical persisted signatures → deterministic slots); `failure-classification.ts` (transient regex registry + two-tier signature derivation); idle-timeout resetting on stdout liveness + 60-min hard ceiling replacing the one-shot wall-clock SIGTERM; partial-artifact salvage (all expected outputs on disk + glob-gate pass → step completes via salvage, `.gitkeep`-guarded); `loadConfig` now parses `timeouts` and all 4 `resolveStepTimeout` call sites receive it. 681 tests. PR #27 (Sprint 12); TEAM.md retro rules via PR #28
- [x] orchestrator-recovery-after-mixed-completion: Feature marked complete at terminal per-feature step; `deriveSprintStatus` lands mixed sprints in `escalated`; `resume_sprint --action=request-changes [--feature=<slug>]` re-engages the escalated feature with attempts reset and feedback injected. PR #21 (Sprint 10)
- [x] expected-outputs-glob-resolution: Real glob matching for `expectedOutputs` validation — new `src/orchestrator/glob-match.ts` (`picomatch` + in-house files-only walker) replaces the `pattern.replace("*", featureSlug)` literal substitution. Exact patterns require an existing FILE; single-star patterns require ≥1 real slug-associated file; double-star patterns match any depth. Missing outputs reported by original pattern string; agent "REQUIRED OUTPUT FILES" instructions generated from the same module so instruction and gate cannot drift; `buildStepContext` input resolution consolidated onto the matcher (also eliminating its unguarded EISDIR read — the true Sprint-8 crash site). QA BDD scenarios salvaged from the escalated Sprint 11 run. PR #24 (Sprint 11, landed via hotfix workflow — bootstrap paradox blocked the orchestrator path)
- [x] artifact-injection-directory-handling: Explicit `statSync().isFile()` gate in `resolveArtifacts` — a directory (or symlink-to-directory) at a resolved artifact path is intentionally treated as absent (required → missing, optional → skipped) instead of relying on `readFileSync` exception fallthrough; symlink-to-file reads normally. 14 salvaged BDD scenarios pinned as an integration suite. Includes provenance correction: the original `artifact-injection.ts:84` crash mapping was wrong (guarded since first commit); actual crash site was `buildStepContext`, fixed in PR #24. PR #25 (Sprint 11, landed via hotfix workflow)
- [x] dev-loop-rebuild-friction: Switch dev loop to `tsx` for direct TS execution. `.mcp.json` checked into repo, runs `npx tsx src/index.ts` — edits to `src/**/*.ts` take effect on next `/mcp` reconnect with no `npm run build` step. Production builds still go through `tsc` → `dist/` for the `bin` entry. Template, CLAUDE.md, and `dev` / `dev:smoke` npm scripts updated. PR #17 (Sprint 9)
- [x] live-claude-smoke-test: Single non-mocked Jest test that spawns the real `claude` CLI with production spawn args; asserts exit=0, non-empty stdout, no stdin-warning marker, no permission-denied marker. Skip-aware (`RAPTOR_SKIP_LIVE_CLAUDE` or `claude` not on `PATH`). Surfaced and triggered a production hardening: PR #18 inserted `--` end-of-options separator before `taskDescription` in `spawnAgent` argv to prevent `--allowedTools` (Commander variadic) from silently absorbing the prompt positional. PRs #18 + #19 (Sprint 9, landed via hotfix workflow due to orchestrator-recovery gap)
- [x] multi-feature-sprint-dispatch: Wire `multi-runner.ts` helpers into `runSprintFromStep` so every backlog item in a sprint section gets dispatched. Per-feature branches, streaming checkpoints, failure isolation. Bundled `sprint-branch-auto-create` and `request-changes-feedback-injection` (Sprint 8)
- [x] sprint-branch-auto-create: Bundled into `multi-feature-sprint-dispatch` — `ensureFeatureBranch()` in `src/orchestrator/multi-runner.ts` is called from both single- and multi-feature paths, replacing the previous "record whatever HEAD points to" logic at `runner.ts:282-306` (Sprint 8)
- [x] request-changes-feedback-injection: Single-feature and multi-feature `request-changes` branches in `resumeSprint` both reset `attempts = 0` and `failures = []` so the retry loop re-enters at attempt 1 and the feedback-injection condition fires (Sprint 8)
- [x] codebase-aware-agent-context: Codebase snapshot (tree, exports, excerpts, deps) injected into agent prompts per step. 30KB cap, .gitignore-aware (Sprint 7)
- [x] read-before-write-enforcement: Required artifacts read from disk and injected into agent task descriptions with pre-generation checklist (Sprint 7)
- [x] progressive-retry-with-scope-narrowing: 3rd retry auto-decomposes by acceptance criteria (Engineer), scenario group (QA), or component (Architect) (Sprint 7)
- [x] adopt-existing-project: Register existing repos in Raptor, scaffold only missing pieces, discover project context (Sprint 6)
- [x] agent-timeout-scaling-by-step-type: Step-aware timeout scaling — QA 15min, Engineer 10min, Architect 7min, configurable cascade (Sprint 6)
- [x] scoped-test-execution: Feature-scoped test commands during implementation, full suite at review only, framework auto-detection (Sprint 6)
- [x] agent-parallel-execution: Parallel agent execution where TEAM.md allows it (Sprint 5)
- [x] multi-engineer-coordination: Multi-engineer support with feature branch isolation and conflict resolution (Sprint 5)
- [x] dino-agent-names: Dinosaur-themed names for each agent role (Sprint 5)
- [x] cross-sprint-context: Sprint summary artifacts generated after each sprint, fed as context to future sprint agents (Sprint 4)
- [x] agent-retrospective-improvements: Each role proposes TEAM.md improvements; user reviews at new retro checkpoint; adopted changes applied (Sprint 4)
- [x] dod-checklist-tracking: Track DoD checklist in sprint state and update PR description before merge (Sprint 4)
- [x] agent-failure-recovery: Circuit breaker (3 retries), structured retry with progressive context, resume from failed/escalated state (Sprint 3)
- [x] sprint-completion-on-merge: PR merge as sprint exit gate — auto-merge after demo approval, sprint ends on merge (Sprint 3)
- [x] mcp-agent-orchestration: Core orchestration loop with run_sprint and resume_sprint MCP tools, user checkpoints, sprint state persistence (Sprint 2)
- [x] mcp-project-bootstrap: Raptor MCP server with bootstrap_project, list_projects, and get_project_status tools (Sprint 1)

## Sprint 14 Demo Feedback — adversarial-verifier-review-gate (2026-07-07)

**Demo:** docs/demos/sprint-14-adversarial-verifier-review-gate.md (on `sprint-14/adversarial-verifier-review-gate`, landed via PR #32)
**PR:** #32 (MERGED 2026-07-07 — merged manually by the user at the demo checkpoint, merge commit 4924d8c)

**Stakeholder verdict (verbatim, demo-feedback checkpoint):** "Demo accepted — 780/780 green, Part 1 + Part 2 shipped, Part 3 vacuous by design. Step 9 note: PR #32 was already merged manually by the user (merge commit 4924d8c) — treat the already-merged response as success per the Sprint 13 state-verifying merge rule, not a failure. Petra at step 10: confirm the invalid-model-classification follow-up is filed to Inbox (unknown `--model` rides the generic retry path today; candidate to short-circuit as user-actionable like `billing-error-signature-class`), and verify `billing-error-signature-class` + `push-before-merge` are still queued in Ready for Sprint 15 consideration."

| Finding | PO triage | Outcome |
|---|---|---|
| 1. Acceptance vs spec intent | ACCEPTED — 17/17 ACs covered (Part 1 + Part 2 shipped, Part 3 vacuous with the decision recorded per AC 11, not a silent skip), 780/780 tests green live, real-seam coverage with RED-verification notes, no regressions across the Sprint 12/13 lineage | Sprint 14 closed |
| 2. Already-merged-PR handling (stakeholder step-9 note) | NO NEW ITEM — the same already-merged-as-success path exercised in Sprint 13 (PRs #29/#30) held again for PR #32; `merge-step-already-merged-idempotency` (Inbox) already tracks first-classing it with a test | — |
| 3. `invalid-model-signature-class` follow-up (stakeholder directive; also flagged in demo §5 item 3 and deferred at tech-approval) | FILE in Inbox — an unknown `--model` burns the full 3-attempt circuit-breaker today; warrants a 1-attempt user-actionable/config escalation class, sibling to `billing-error-signature-class`, extending the same classification pipeline | Filed in Inbox |
| 4. Confirm `billing-error-signature-class` + `push-before-merge` still queued for Sprint 15 (stakeholder directive) | CONFIRMED — both remain in Ready (`push-before-merge` #2, `billing-error-signature-class` #3), unchanged by this sprint | — |
| 5. Anything else | None — Part 3 explicitly vacuous, no judge ensemble built (research refutation honored); no defects filed (full suite green) | — |

**Net backlog mutations:**
- Sprint 14 — Planned: `adversarial-verifier-review-gate` checked, moved to Done (PR #32)
- Added to Inbox: `invalid-model-signature-class`
- Ready unchanged: `push-before-merge` (#2) and `billing-error-signature-class` (#3) confirmed still queued for Sprint 15

## Sprint 13 Demo Feedback — sprint-completes-despite-failed-merge (2026-07-06)

**Demo:** docs/demos/sprint-13-sprint-completes-despite-failed-merge.md (on `sprint-13/sprint-completes-despite-failed-merge`, landed via PR #29)
**PR:** #29 (MERGED 2026-07-06T22:28Z — merged manually by the user at the demo checkpoint, merge commit c07eeb9)

**Stakeholder verdict (verbatim, demo-feedback checkpoint):** "Demo accepted — 709/709 green live, DoD satisfied, and the demo avoided the blocker-marker false positive this time. Note for step 9: PR #29 was already merged manually by the user (merge commit c07eeb9 on main at 2026-07-06T22:28Z), so the merge step will encounter an already-merged PR — treat a 'pull request already merged' response as success (the code is verifiably on main), not as a merge failure."

| Finding | PO triage | Outcome |
|---|---|---|
| 1. `push-before-merge` (demo §6 + Architect OQ1 ruling; filing directed by user at tech-approval checkpoint, re-confirmed in feature-2 demo feedback) | PROMOTE to Ready, #2 in queue — root-causes the Sprint 12 divergence failure; Anky sized it ~10 lines + tests (push failures / no-remote / force-push hazards). **Sprint 14 candidate** | Added to Ready (#2) |
| 2. `billing-error-signature-class` (feature-2 demo feedback, directed to PO at step 10; incidents span both features — commits 908bf63, 9394bdd, f9bc035) | PROMOTE to Ready, #3 — hit this sprint three times; retrying a spend-limit error is pure waste until the user acts, so it warrants a 1-attempt escalation class | Added to Ready (#3) |
| 3. `merge-failure-short-circuit` (demo §6; Architect OQ2 deferral — C5 makes a future short-circuit state-compatible) | DEFER to Inbox — merge retries cost seconds; no friction data yet (precedent: `batch-checkpoints-config`) | Filed in Inbox |
| 4. Already-merged-PR handling (stakeholder step-9 note, applied to both PRs #29/#30) | FILE `merge-step-already-merged-idempotency` in Inbox — verify `executeMerge` treats PR state MERGED as success natively; make it first-class if the Sprint 13 pass relied on shepherding | Filed in Inbox |
| 5. `sprint-result-status-hardcoded-escalated` (demo §6: adjacent, not touched by this PR) | NO ACTION — remains in Inbox as filed at Sprint 10 | — |

**Net backlog mutations:**
- Sprint 13 — Planned: `sprint-completes-despite-failed-merge` checked, moved to Done (PR #29)
- Added to Ready: `push-before-merge` (#2), `billing-error-signature-class` (#3)
- Added to Inbox: `merge-failure-short-circuit`, `merge-step-already-merged-idempotency`
- Retired Ready → Done: `sprint-11-write-tests-escalation` post-mortem (deferred instruction from Sprint 12 feedback; PR #27 merged 2026-07-06)
- `retro-improvements-not-applied` (Sprint 13 feature 2) left for its own step-10 pass — PR #30 merged 2026-07-07, feedback recorded in checkpoint state

## Sprint 12 Demo Feedback (2026-07-06)

**Demo:** [docs/demos/sprint-12-progress-aware-circuit-breaker.md](demos/sprint-12-progress-aware-circuit-breaker.md)
**PR:** #27 (open — merge pending; item moves to Done on merge per `sprint-completion-on-merge`)

**Stakeholder verdict:** Demo excellent; content accepted as committed in `0173ceb`. The post-demo `[ESCALATE]` (commit b4c5ffb) was a confirmed FALSE POSITIVE — the presentation quoted the decision-pipeline diagram containing the literal blocker marker and `hasBlockerMarker` matched it. Demo work stands; no redo required.

| Finding | PO triage | Outcome |
|---|---|---|
| 1. `salvage-checkpoint-review-flag` (spec Open Question 6: force human review after salvage-complete?) | DEFER — salvage already gated on all outputs existing + glob-valid, and `completedVia: "salvage"` is persisted/auditable. No approval friction until friction data exists (precedent: `batch-checkpoints-config`) | Filed in Inbox |
| 2. `config-keys-parsed-vs-declared` (`loadConfig` drops `testConfig`, `codebaseContext`, `artifactInjection`, `scopeNarrowing`) | PROMOTE to Ready, mid-queue — same silent-config-drop class as the `timeouts` plumbing CB-5 just fixed, which went unnoticed for six sprints | Added to Ready |
| 3. `blocker-marker-false-positive-in-agent-output` (naive `hasBlockerMarker` match escalated this sprint's own demo) | PROMOTE to Ready, TOP of queue — hit the sprint itself, live false escalation (precedent: Sprint 8 question #4) | Added to Ready (#1) |
| 4. Architect constants (60-min ceiling, 5 transient retries/step, 15 s delay, idle/ceiling kills = deterministic, zero new deps) | CONFIRMED as demoed — no backlog action | — |
| 5. Anything else | Step-5 infra escalations (3× 10-min timeouts, 3× spend-limit errors after green implementation) noted as live evidence for CB-2/CB-3 — no new item, the shipped feature is the fix | — |

**Net backlog mutations:**
- Added to Ready (top): `blocker-marker-false-positive-in-agent-output`
- Added to Ready (mid-queue, after `reset-sprint-tool`): `config-keys-parsed-vs-declared`
- Added to Inbox: `salvage-checkpoint-review-flag` (deferred spec Open Question 6)
- Sprint 12 — Planned item left `- [ ]`: PR #27 not yet merged; acceptance criteria unchanged
- `sprint-11-write-tests-escalation` (Ready) retained as post-mortem record — retire to Done alongside `progress-aware-circuit-breaker` once PR #27 merges

## Sprint 8 Demo Feedback (2026-04-27)

**Demo:** [docs/demos/sprint-8-multi-feature-sprint-dispatch.md](demos/sprint-8-multi-feature-sprint-dispatch.md)
**PR:** #15 (merged dc869a2)

| Demo question | PO triage | Outcome |
|---|---|---|
| 1. Acceptance vs spec intent | ACCEPTED — 14/14 ACs covered, 435/435 tests pass, no regressions | Sprint 8 closed |
| 2. Bundling `sprint-branch-auto-create` + `request-changes-feedback-injection` | ACCEPTED — shared code path, single review surface | Both items moved to Done |
| 3. Streaming checkpoints (one-per-feature, sequential) | CONFIRMED as designed — defer batching until friction data exists | Filed `batch-checkpoints-config` in Inbox |
| 4. Deferred plumbing bugs (`expected-outputs-glob-resolution`, `artifact-injection-directory-handling`) | PROMOTE both Inbox → Ready, top of queue for Sprint 9 — both hit Sprint 8 itself and masked correct QA output | Promoted |
| 5. Anything else | None | — |

**Net backlog mutations:**
- Promoted Inbox → Ready: `expected-outputs-glob-resolution`, `artifact-injection-directory-handling`
- Added to Inbox: `batch-checkpoints-config` (deferred follow-up from AC #13)
- Sprint 8 — Planned section emptied (all items in Done)
